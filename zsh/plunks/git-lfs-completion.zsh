
# adds the "git lfs" completion
# "git-lfs" should be autoloaded by compinit and fpath
# autoload -Uz _git-lfs
# zstyle ':completion:*:*:git:*' user-commands lfs:'access to large file storage controls'
